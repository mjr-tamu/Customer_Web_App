<div class="home-container">
  <div class="calendar-header">
    <div class="header-content">
      <div class="header-left">
        <h1>WIPS Event Calendar</h1>
        <% if admin_signed_in? %>
          <p>Welcome back, <%= current_admin.email %></p>
        <% elsif user_signed_in? %>
          <p>Welcome, <%= current_user["name"] %>!</p>
        <% else %>
          <p>Public Event Calendar</p>
        <% end %>
      </div>
      <div class="header-right">
        <div class="header-actions">
          <%= link_to "About", about_path, class: "about-btn" %>
          <% if admin_signed_in? %>
            <%= link_to "Sign Out", destroy_admin_session_path, method: :delete, class: "sign-out-btn" %>
            <%= link_to admin_management_path, class: "admin-menu-btn", title: "Admin Management" do %>
              <span class="hamburger-icon">☰</span>
            <% end %>
          <% elsif user_signed_in? %>
            <%= button_to "Sign Out", sign_out_user_path, method: :delete, class: "sign-out-btn", 
                data: { confirm: "Are you sure you want to sign out?" } %>
          <% else %>
            <%= button_to "Sign In", admin_google_oauth2_omniauth_authorize_path, method: :post, class: "sign-in-btn", data: {turbo: "false"} %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- Category Filter Section -->
  <div class="category-filter">
    <div class="filter-buttons">
      <%= link_to "All", home_path(date: params[:date]), 
          class: "filter-btn #{'active' if @selected_categories.empty?}" %>
      <% @available_categories.each do |category| %>
        <% 
          # Toggle category in selection
          if @selected_categories.include?(category)
            new_categories = @selected_categories - [category]
          else
            new_categories = @selected_categories + [category]
          end
        %>
        <%= link_to category, home_path(categories: new_categories, date: params[:date]), 
            class: "filter-btn #{'active' if @selected_categories.include?(category)}" %>
      <% end %>
    </div>
    <% if @selected_categories.any? %>
      <div class="active-filters">
        <small>Active filters: <%= @selected_categories.join(', ') %></small>
      </div>
    <% end %>
  </div>

  <div class="main-content <%= 'with-dashboard' if user_signed_in? && !admin_signed_in? %>">
    <div class="calendar-container">
      <div class="calendar-nav">
        <div class="nav-left">
          <%= link_to "←", home_path(date: (@current_date - 1.month).strftime("%Y-%m")), class: "nav-btn prev" %>
          <%= link_to "→", home_path(date: (@current_date + 1.month).strftime("%Y-%m")), class: "nav-btn next" %>
          <h2><%= @current_date.strftime("%B %Y") %></h2>
        </div>
        <div class="nav-right">
          <%= link_to "Today", home_path, class: "today-btn" %>
          <% if admin_signed_in? %>
            <%= link_to "Create", new_calendar_path, class: "create-btn" %>
          <% end %>
        </div>
      </div>

      <div class="calendar-grid">
    <div class="calendar-header-row">
      <div class="day-header">Sun</div>
      <div class="day-header">Mon</div>
      <div class="day-header">Tue</div>
      <div class="day-header">Wed</div>
      <div class="day-header">Thu</div>
      <div class="day-header">Fri</div>
      <div class="day-header">Sat</div>
    </div>
    
    <% @calendar_days.each_slice(7) do |week| %>
      <div class="calendar-week">
        <% week.each do |day| %>
          <div class="calendar-day <%= 'other-month' if day[:other_month] %> <%= 'today' if day[:today] %>">
            <div class="day-number"><%= day[:date].day %></div>
            <div class="events">
              <% day[:events].each do |event| %>
                <div class="event-item" title="<%= event.title %> - <%= event.event_date.strftime('%I:%M %p') %> - <%= event.category %>">
                  <%= link_to truncate(event.title, length: 15), show_calendar_path(event), class: "event-link" %>
                  <div class="event-category"><%= event.category %></div>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
    </div>
  </div>

  <!-- Member Dashboard (only for signed-in users, not admins) -->
  <% if user_signed_in? && !admin_signed_in? %>
    <div class="member-dashboard">
      <div class="dashboard-header">
        <h3>My Events</h3>
        <p>Events you've signed up for</p>
      </div>
      
      <div class="dashboard-content">
        <% if @user_signups.any? %>
          <div class="signed-up-events">
            <% @user_signups.each do |signup| %>
              <div class="event-card">
                <div class="event-info">
                  <h4><%= signup.calendar.title %></h4>
                  <p class="event-date">
                    <%= signup.calendar.event_date.strftime("%B %d, %Y") %>
                    at <%= signup.calendar.event_date.strftime("%I:%M %p") %>
                  </p>
                  <p class="event-category"><%= signup.calendar.category %></p>
                  <% if signup.calendar.location.present? %>
                    <p class="event-location">📍 <%= signup.calendar.location %></p>
                  <% end %>
                </div>
                <div class="event-actions">
                  <%= link_to "View Details", show_calendar_path(signup.calendar), class: "view-details-btn" %>
                  <%= button_to "Cancel", cancel_signup_calendar_path(signup.calendar), method: :delete, 
                      class: "cancel-signup-btn", 
                      data: { confirm: "Are you sure you want to cancel your signup for this event?" } %>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="no-events">
            <p>You haven't signed up for any events yet.</p>
            <p>Browse the calendar and click on events to sign up!</p>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<style>
body {
  font-family: 'Roboto', 'Google Sans', Arial, sans-serif;
  background: linear-gradient(135deg, #ffccd5 0%, #ff8fab 25%, #ffffff 50%, #ffccd5 75%, #ff8fab 100%);
  background-size: 400% 400%;
  animation: gradientShift 15s ease infinite;
  margin: 0;
  padding: 0;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.home-container {
  width: 100%;
  max-width: 1400px;
  padding: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.main-content {
  width: 100%;
}

.main-content.with-dashboard {
  display: flex;
  gap: 30px;
  align-items: flex-start;
}

.calendar-container {
  width: 100%;
}

.main-content.with-dashboard .calendar-container {
  flex: 1;
  min-width: 0;
}

.calendar-header {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  padding: 20px 30px;
  margin-bottom: 30px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  width: 100%;
}

.header-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
}

.header-left h1 {
  margin: 0;
  font-size: 1.8rem;
  font-weight: 300;
  color: #d81b60;
  text-shadow: 0 2px 4px rgba(216, 27, 96, 0.1);
}

.header-left p {
  margin: 5px 0 0 0;
  font-size: 0.9rem;
  color: #5f6368;
  font-weight: 400;
}

.header-right {
  display: flex;
  align-items: center;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 15px;
}

.admin-menu-btn {
  color: #5f6368;
  text-decoration: none;
  padding: 10px;
  border: 2px solid #dadce0;
  border-radius: 50%;
  font-size: 1.2rem;
  font-weight: 500;
  transition: all 0.3s ease;
  background: white;
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
}

.admin-menu-btn:hover {
  background: #ffccd5;
  border-color: #ff8fab;
  color: #d81b60;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(255, 143, 171, 0.2);
}

.hamburger-icon {
  font-size: 1.4rem;
  font-weight: bold;
}

.sign-out-btn {
  color: #5f6368;
  text-decoration: none;
  padding: 10px 20px;
  border: 2px solid #dadce0;
  border-radius: 50px;
  font-size: 0.9rem;
  font-weight: 500;
  transition: all 0.3s ease;
  background: white;
}

.sign-out-btn:hover {
  background: #ffccd5;
  border-color: #ff8fab;
  color: #d81b60;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(255, 143, 171, 0.2);
}

.sign-in-btn {
  color: #5f6368;
  text-decoration: none;
  padding: 10px 20px;
  border: 2px solid #dadce0;
  border-radius: 50px;
  font-size: 0.9rem;
  font-weight: 500;
  transition: all 0.3s ease;
  background: white;
}

.sign-in-btn:hover {
  background: #ffccd5;
  border-color: #ff8fab;
  color: #d81b60;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(255, 143, 171, 0.2);
}

.about-btn {
  color: #5f6368;
  text-decoration: none;
  padding: 10px 20px;
  border: 2px solid #dadce0;
  border-radius: 50px;
  font-size: 0.9rem;
  font-weight: 500;
  transition: all 0.3s ease;
  background: white;
  margin-right: 10px;
}

.about-btn:hover {
  background: #ffccd5;
  border-color: #ff8fab;
  color: #d81b60;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(255, 143, 171, 0.2);
}

.category-filter {
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10px);
  border-radius: 15px;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
  padding: 15px 25px;
  margin-bottom: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  width: 100%;
}

.filter-buttons {
  display: flex;
  gap: 10px;
  justify-content: center;
  flex-wrap: wrap;
}

.filter-btn {
  padding: 8px 16px;
  border: 2px solid #e0e0e0;
  border-radius: 20px;
  background: white;
  color: #5f6368;
  text-decoration: none;
  font-weight: 500;
  font-size: 0.9rem;
  transition: all 0.3s ease;
  cursor: pointer;
}

.filter-btn:hover {
  border-color: #ff8fab;
  background: #ffccd5;
  color: #3c4043;
  transform: translateY(-1px);
}

.filter-btn.active {
  background: #ff8fab;
  border-color: #ff8fab;
  color: white;
  box-shadow: 0 4px 15px rgba(255, 143, 171, 0.3);
}

.active-filters {
  text-align: center;
  margin-top: 10px;
  color: #5f6368;
  font-size: 0.8rem;
}

.calendar-container {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  padding: 30px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  width: 100%;
  max-width: 1000px;
}

.calendar-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
  padding-bottom: 16px;
  border-bottom: 1px solid #dadce0;
}

.nav-left {
  display: flex;
  align-items: center;
  gap: 16px;
}

.nav-btn {
  color: #5f6368;
  text-decoration: none;
  padding: 8px;
  border: 1px solid #dadce0;
  border-radius: 4px;
  font-size: 18px;
  font-weight: 500;
  transition: all 0.2s ease;
  background: white;
  min-width: 40px;
  text-align: center;
  display: inline-block;
}

.nav-btn:hover {
  background: #f8f9fa;
  border-color: #5f6368;
}

.nav-left h2 {
  margin: 0;
  color: #3c4043;
  font-size: 22px;
  font-weight: 400;
}

.nav-right {
  display: flex;
  gap: 8px;
}

.today-btn, .create-btn {
  color: #5f6368;
  text-decoration: none;
  padding: 8px 16px;
  border: 1px solid #dadce0;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 500;
  transition: all 0.2s ease;
  background: white;
}

.today-btn:hover, .create-btn:hover {
  background: #f8f9fa;
  border-color: #5f6368;
}

.create-btn {
  background: #ffccd5 !important;
  border-color: #ff8fab !important;
  color: #d81b60 !important;
}

.create-btn:hover {
  background: #ff8fab !important;
  color: white !important;
}

.calendar-grid {
  width: 100%;
  border: 1px solid #dadce0;
  border-radius: 8px;
  overflow: hidden;
}

.calendar-header-row {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  background: #f8f9fa;
  border-bottom: 1px solid #dadce0;
}

.day-header {
  padding: 16px 8px;
  text-align: center;
  font-weight: 500;
  color: #5f6368;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  border-right: 1px solid #dadce0;
}

.day-header:last-child {
  border-right: none;
}

.calendar-week {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  border-bottom: 1px solid #dadce0;
}

.calendar-week:last-child {
  border-bottom: none;
}

.calendar-day {
  min-height: 100px;
  background: white;
  border-right: 1px solid #dadce0;
  padding: 8px;
  position: relative;
  transition: background-color 0.2s ease;
}

.calendar-day:last-child {
  border-right: none;
}

.calendar-day:hover {
  background: #f8f9fa;
}

.calendar-day.other-month {
  background: #fafafa;
  color: #9aa0a6;
}

.calendar-day.today {
  background: #fff3e0;
}

.calendar-day.today .day-number {
  background: #ffccd5;
  color: #d81b60;
  border-radius: 50%;
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 500;
  font-size: 14px;
}

.day-number {
  font-weight: 400;
  margin-bottom: 4px;
  color: #3c4043;
  font-size: 14px;
}

.events {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.event-item {
  background: #ffebee;
  border-left: 3px solid #ff8fab;
  padding: 2px 6px;
  border-radius: 3px;
  font-size: 12px;
  line-height: 1.3;
}

.event-link {
  color: #d81b60;
  text-decoration: none;
  font-weight: 400;
}

.event-link:hover {
  color: #ad1457;
  text-decoration: underline;
}

.event-category {
  font-size: 10px;
  color: #666;
  font-weight: 500;
  margin-top: 1px;
}

@media (max-width: 768px) {
  .home-container {
    padding: 15px;
  }
  
  .calendar-header {
    padding: 20px;
    margin-bottom: 20px;
  }
  
  .calendar-container {
    padding: 20px;
  }
  
  .calendar-nav {
    flex-direction: column;
    gap: 16px;
    align-items: stretch;
  }
  
  .nav-left, .nav-right {
    justify-content: center;
  }
  
  .calendar-day {
    min-height: 80px;
    font-size: 12px;
  }
  
  .day-header {
    padding: 12px 4px;
    font-size: 10px;
  }
  
  .header-content {
    flex-direction: column;
    gap: 12px;
    align-items: stretch;
  }
  
  .header-left {
    text-align: center;
  }
  
  .header-right {
    justify-content: center;
  }
  
  .header-actions {
    gap: 10px;
  }
  
  .admin-menu-btn {
    width: 40px;
    height: 40px;
    padding: 8px;
  }
}

/* Member Dashboard Styles */
.member-dashboard {
  width: 350px;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  padding: 0;
  height: fit-content;
  max-height: 80vh;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.dashboard-header {
  padding: 20px 25px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.3);
  background: rgba(255, 255, 255, 0.8);
}

.dashboard-header h3 {
  margin: 0 0 5px 0;
  color: #d81b60;
  font-size: 1.3rem;
  font-weight: 400;
}

.dashboard-header p {
  margin: 0;
  color: #5f6368;
  font-size: 0.9rem;
}

.dashboard-content {
  flex: 1;
  overflow-y: auto;
  padding: 15px 20px;
}

.signed-up-events {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.event-card {
  background: rgba(255, 255, 255, 0.8);
  border-radius: 12px;
  padding: 15px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  transition: all 0.3s ease;
}

.event-card:hover {
  background: rgba(255, 255, 255, 0.95);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(216, 27, 96, 0.1);
}

.event-info h4 {
  margin: 0 0 8px 0;
  color: #d81b60;
  font-size: 1rem;
  font-weight: 500;
  line-height: 1.3;
}

.event-info p {
  margin: 0 0 5px 0;
  color: #5f6368;
  font-size: 0.85rem;
  line-height: 1.4;
}

.event-date {
  font-weight: 500;
  color: #333 !important;
}

.event-category {
  background: #ffccd5;
  color: #d81b60;
  padding: 2px 8px;
  border-radius: 12px;
  font-size: 0.75rem !important;
  font-weight: 500;
  display: inline-block;
  margin: 5px 0 !important;
}

.event-location {
  font-size: 0.8rem !important;
  color: #666 !important;
}

.event-actions {
  display: flex;
  gap: 8px;
  margin-top: 10px;
}

.view-details-btn, .cancel-signup-btn {
  flex: 1;
  text-align: center;
  padding: 6px 12px;
  border-radius: 20px;
  font-size: 0.8rem;
  font-weight: 500;
  text-decoration: none;
  transition: all 0.3s ease;
  border: 1px solid transparent;
}

.view-details-btn {
  background: #d81b60;
  color: white;
  border-color: #d81b60;
}

.view-details-btn:hover {
  background: #b71c50;
  color: white;
  text-decoration: none;
  transform: translateY(-1px);
}

/* New Dashboard Cancel Button Styles */
.cancel-signup-btn .button_to {
  display: inline-block;
  margin: 0;
  padding: 0;
}

.cancel-signup-btn button {
  background: transparent;
  color: #666;
  border: 1px solid #ddd;
  border-radius: 20px;
  padding: 6px 12px;
  font-size: 0.8rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.3s ease;
  display: inline-block;
  width: 100%;
  font-family: inherit;
  text-decoration: none;
}

.cancel-signup-btn button:hover {
  background: #ffebee;
  color: #d32f2f;
  border-color: #ffcdd2;
}

.no-events {
  text-align: center;
  padding: 40px 20px;
  color: #5f6368;
}

.no-events p {
  margin: 0 0 10px 0;
  font-size: 0.9rem;
  line-height: 1.5;
}

.no-events p:last-child {
  font-size: 0.8rem;
  color: #666;
}

@media (max-width: 1200px) {
  .main-content.with-dashboard {
    flex-direction: column;
  }
  
  .member-dashboard {
    width: 100%;
    max-height: none;
  }
}

@media (max-width: 768px) {
  .home-container {
    padding: 15px;
  }
  
  .main-content {
    gap: 20px;
  }
  
  .member-dashboard {
    width: 100%;
  }
  
  .dashboard-header {
    padding: 15px 20px;
  }
  
  .dashboard-content {
    padding: 15px;
  }
  
  .event-actions {
    flex-direction: column;
  }
  
  .view-details-btn, .cancel-signup-btn {
    padding: 8px 12px;
  }
}
</style>
