<div class="details-container">
  <div class="details-card">
    <div class="details-header">
      <div class="details-icon">
        📅
      </div>
      <h1 class="details-title">Event Details</h1>
      <p class="details-subtitle">View your calendar event information</p>
    </div>

    <div class="details-content">
      <div class="event-info">
        <div class="info-section">
          <div class="info-label">
            <span class="label-icon">📝</span>
            Event Title
          </div>
          <div class="info-value"><%= @calendar.title %></div>
        </div>

        <div class="info-section">
          <div class="info-label">
            <span class="label-icon">📅</span>
            Date & Time
          </div>
          <div class="info-value">
            <%= @calendar.event_date.strftime("%A, %B %d, %Y") %>
            <br>
            <span class="time-info"><%= @calendar.event_date.strftime("%I:%M %p") %></span>
          </div>
        </div>

        <div class="info-section">
          <div class="info-label">
            <span class="label-icon">🏷️</span>
            Category
          </div>
          <div class="info-value"><%= @calendar.category %></div>
        </div>

        <div class="info-section">
          <div class="info-label">
            <span class="label-icon">📄</span>
            Description
          </div>
          <div class="info-value description-text"><%= @calendar.description %></div>
        </div>

        <div class="info-section">
          <div class="info-label">
            <span class="label-icon">📍</span>
            Location
          </div>
          <div class="info-value"><%= @calendar.location.present? ? @calendar.location : 'No location specified' %></div>
        </div>
      </div>

      <div class="action-buttons">
        <% if user_signed_in? %>
          <% if current_user.signed_up_events.include?(@calendar) %>
            <%= button_to 'Sign Out', signout_calendar_path(@calendar), method: :delete, class: "action-button signout-button", 
                data: { confirm: "Are you sure you want to sign out of this event?" } %>
          <% else %>
            <%= button_to 'Sign Up', signup_calendar_path(@calendar), method: :post, class: "action-button signup-button" %>
          <% end %>
        <% end %>
        
        <% if admin_user? %>
          <%= link_to 'Edit Event', edit_calendar_path(@calendar), class: "action-button edit-button" %>
          <%= link_to 'Export CSV', export_calendar_path(@calendar, format: :csv), class: "action-button export-button" %>
          <button type="button" class="action-button delete-button" onclick="openDeleteModal()">Delete Event</button>
        <% end %>
        <%= link_to 'Back to Calendar', home_path, class: "action-button back-button" %>
      </div>
    </div>
  </div>
</div>

<!-- Delete Confirmation Modal -->
<div id="deleteModal" class="modal">
  <div class="modal-content">
    <div class="modal-header">
      <h3>Confirm Event Deletion</h3>
      <span class="close" onclick="closeDeleteModal()">&times;</span>
    </div>
    <div class="modal-body">
      <p>Are you sure you want to delete <strong>"<%= @calendar.title %>"</strong>?</p>
      <p class="warning-text">This action cannot be undone and will remove all signup data for this event.</p>
    </div>
    <div class="modal-footer">
      <button type="button" class="modal-btn cancel-btn" onclick="closeDeleteModal()">Cancel</button>
      <%= button_to "Delete Event", destroy_calendar_path(@calendar), method: :delete, class: "modal-btn delete-btn", data: { confirm: false } %>
    </div>
  </div>
</div>

<style>
body {
  font-family: 'Roboto', 'Google Sans', Arial, sans-serif;
  background: linear-gradient(135deg, #ffccd5 0%, #ff8fab 25%, #ffffff 50%, #ffccd5 75%, #ff8fab 100%);
  background-size: 400% 400%;
  animation: gradientShift 15s ease infinite;
  margin: 0;
  padding: 20px;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.details-container {
  width: 100%;
  max-width: 700px;
  padding: 20px;
}

.details-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  padding: 40px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.details-header {
  text-align: center;
  margin-bottom: 40px;
  border-bottom: 2px solid #ffccd5;
  padding-bottom: 30px;
}

.details-icon {
  font-size: 4rem;
  margin-bottom: 20px;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}

.details-title {
  font-size: 2.5rem;
  font-weight: 300;
  color: #d81b60;
  margin: 0 0 10px 0;
  text-shadow: 0 2px 4px rgba(216, 27, 96, 0.1);
}

.details-subtitle {
  font-size: 1.1rem;
  color: #5f6368;
  margin: 0;
  font-weight: 400;
}

.details-content {
  margin-top: 30px;
}

.event-info {
  display: flex;
  flex-direction: column;
  gap: 25px;
  margin-bottom: 40px;
}

.info-section {
  background: rgba(255, 204, 213, 0.2);
  border-radius: 15px;
  padding: 20px;
  border: 1px solid rgba(255, 143, 171, 0.2);
  transition: all 0.3s ease;
}

.info-section:hover {
  background: rgba(255, 204, 213, 0.3);
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(255, 143, 171, 0.2);
}

.info-label {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1rem;
  font-weight: 500;
  color: #d81b60;
  margin-bottom: 10px;
}

.label-icon {
  font-size: 1.2rem;
}

.info-value {
  font-size: 1.1rem;
  color: #3c4043;
  line-height: 1.5;
  margin-left: 32px;
}

.time-info {
  color: #5f6368;
  font-weight: 500;
}

.description-text {
  background: white;
  padding: 15px;
  border-radius: 8px;
  border: 1px solid #e0e0e0;
  margin-top: 5px;
}

.action-buttons {
  display: flex;
  gap: 15px;
  justify-content: center;
  flex-wrap: wrap;
  margin-top: 30px;
  padding-top: 30px;
  border-top: 2px solid #ffccd5;
}

.action-button {
  padding: 12px 24px;
  border-radius: 50px;
  text-decoration: none;
  font-weight: 500;
  font-size: 1rem;
  transition: all 0.3s ease;
  text-align: center;
  min-width: 140px;
  border: 2px solid;
}

.edit-button {
  background: linear-gradient(135deg, #ff8fab 0%, #ffccd5 100%);
  color: white;
  border-color: #ff8fab;
}

.edit-button:hover {
  background: linear-gradient(135deg, #ff6b92 0%, #ff8fab 100%);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(255, 143, 171, 0.3);
}

.delete-button {
  background: transparent;
  color: #f44336;
  border-color: #f44336;
}

.delete-button:hover {
  background: #f44336;
  color: white;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(244, 67, 54, 0.3);
}

.back-button {
  background: transparent;
  color: #5f6368;
  border-color: #dadce0;
}

.back-button:hover {
  background: #f8f9fa;
  border-color: #5f6368;
  color: #3c4043;
  transform: translateY(-2px);
}

.signup-button {
  background: linear-gradient(135deg, #4caf50 0%, #66bb6a 100%);
  color: white;
  border-color: #4caf50;
}

.signup-button:hover {
  background: linear-gradient(135deg, #388e3c 0%, #4caf50 100%);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(76, 175, 80, 0.3);
}

.signout-button {
  background: transparent;
  color: #ff9800;
  border-color: #ff9800;
}

.signout-button:hover {
  background: #ff9800;
  color: white;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(255, 152, 0, 0.3);
}

.export-button {
  background: linear-gradient(135deg, #2196f3 0%, #42a5f5 100%);
  color: white;
  border-color: #2196f3;
}

.export-button:hover {
  background: linear-gradient(135deg, #1976d2 0%, #2196f3 100%);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(33, 150, 243, 0.3);
}

/* Modal Styles */
.modal {
  display: none;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(5px);
}

.modal-content {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  margin: 15% auto;
  padding: 0;
  border-radius: 20px;
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
  width: 90%;
  max-width: 500px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  animation: modalSlideIn 0.3s ease;
}

@keyframes modalSlideIn {
  from {
    opacity: 0;
    transform: translateY(-50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 25px 30px;
  border-bottom: 2px solid #ffccd5;
  background: linear-gradient(135deg, #ff8fab 0%, #ffccd5 100%);
  border-radius: 20px 20px 0 0;
}

.modal-header h3 {
  margin: 0;
  color: white;
  font-size: 1.4rem;
  font-weight: 300;
}

.close {
  color: white;
  font-size: 28px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
}

.close:hover {
  color: #ff6b92;
  transform: scale(1.1);
}

.modal-body {
  padding: 30px;
}

.modal-body p {
  font-size: 1.1rem;
  color: #3c4043;
  margin: 0 0 15px 0;
  line-height: 1.5;
}

.warning-text {
  color: #f44336;
  font-weight: 500;
  background: rgba(244, 67, 54, 0.1);
  padding: 15px;
  border-radius: 10px;
  border-left: 4px solid #f44336;
}

.modal-footer {
  display: flex;
  gap: 15px;
  justify-content: flex-end;
  padding: 20px 30px 30px;
}

.modal-btn {
  padding: 12px 24px;
  border-radius: 50px;
  border: 2px solid;
  font-weight: 500;
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.3s ease;
  text-decoration: none;
  display: inline-block;
  text-align: center;
}

.cancel-btn {
  background: transparent;
  color: #5f6368;
  border-color: #dadce0;
}

.cancel-btn:hover {
  background: #f8f9fa;
  border-color: #5f6368;
  color: #3c4043;
  transform: translateY(-1px);
}

.delete-btn {
  background: #f44336;
  color: white;
  border-color: #f44336;
}

.delete-btn:hover {
  background: #d32f2f;
  border-color: #d32f2f;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(244, 67, 54, 0.3);
}

@media (max-width: 768px) {
  .details-card {
    padding: 30px 20px;
    margin: 10px;
  }
  
  .details-title {
    font-size: 2rem;
  }
  
  .action-buttons {
    flex-direction: column;
    align-items: center;
  }
  
  .action-button {
    width: 100%;
    max-width: 250px;
  }
  
  .info-value {
    margin-left: 0;
    margin-top: 10px;
  }
}

@media (max-width: 480px) {
  .details-container {
    padding: 10px;
  }
  
  .details-card {
    padding: 25px 15px;
  }
  
  .details-title {
    font-size: 1.8rem;
  }
  
  .details-icon {
    font-size: 3rem;
  }
}
</style>

<script>
function openDeleteModal() {
  document.getElementById('deleteModal').style.display = 'block';
  document.body.style.overflow = 'hidden';
}

function closeDeleteModal() {
  document.getElementById('deleteModal').style.display = 'none';
  document.body.style.overflow = 'auto';
}

// Close modal when clicking outside of it
window.onclick = function(event) {
  const modal = document.getElementById('deleteModal');
  if (event.target == modal) {
    closeDeleteModal();
  }
}

// Close modal with Escape key
document.addEventListener('keydown', function(event) {
  if (event.key === 'Escape') {
    closeDeleteModal();
  }
});
</script>
